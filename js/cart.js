let cart=JSON.parse(localStorage.getItem('haveli-cart')||'[]');const panel=document.getElementById('cartPanel');function saveCart(){localStorage.setItem('haveli-cart',JSON.stringify(cart));renderCart();updateCartCount()}function addToCart(id,qty){const item=cart.find(i=>i.id===id);if(item)item.qty+=qty;else cart.push({id,qty});saveCart();panel.classList.add('open');panel.setAttribute('aria-hidden','false')}function updateCartCount(){document.getElementById('cartCount').textContent=cart.reduce((s,i)=>s+i.qty,0)}function renderCart(){const wrap=document.getElementById('cartItems');if(!cart.length){wrap.innerHTML='<div style="padding:35px 8px;text-align:center;color:#777;font-size:12px">Your cart is waiting for something delicious.</div>'}else{wrap.innerHTML=cart.map(i=>{const p=products.find(x=>x.id===i.id);return `<div class="cart-item"><img src="${p.image}" alt="${p.name}"><div><h4>${p.name}</h4><p>₹${p.price} × ${i.qty}</p><div class="qty-mini"><button data-dec="${p.id}">−</button><span>${i.qty}</span><button data-inc="${p.id}">+</button></div></div><button style="border:0;background:none;color:#b44;cursor:pointer" data-remove="${p.id}">Remove</button></div>`}).join('');wrap.querySelectorAll('[data-inc]').forEach(b=>b.onclick=()=>changeQty(+b.dataset.inc,1));wrap.querySelectorAll('[data-dec]').forEach(b=>b.onclick=()=>changeQty(+b.dataset.dec,-1));wrap.querySelectorAll('[data-remove]').forEach(b=>b.onclick=()=>removeItem(+b.dataset.remove))}const subtotal=cart.reduce((s,i)=>{const p=products.find(x=>x.id===i.id);return s+p.price*i.qty},0);const delivery=subtotal?40:0;document.getElementById('subtotal').textContent=`₹${subtotal}`;document.getElementById('delivery').textContent=`₹${delivery}`;document.getElementById('total').textContent=`₹${subtotal+delivery}`}function changeQty(id,d){const i=cart.find(x=>x.id===id);if(!i)return;i.qty+=d;if(i.qty<=0)cart=cart.filter(x=>x.id!==id);saveCart()}function removeItem(id){cart=cart.filter(x=>x.id!==id);saveCart()}document.getElementById('cartBtn').onclick=()=>{panel.classList.add('open');panel.setAttribute('aria-hidden','false')};document.getElementById('closeCart').onclick=()=>{panel.classList.remove('open');panel.setAttribute('aria-hidden','true')};document.getElementById('cartBackdrop').onclick=()=>{panel.classList.remove('open');panel.setAttribute('aria-hidden','true')};document.getElementById('checkoutBtn').onclick=()=>{alert('Demo checkout: this prototype does not connect to a live payment gateway.')};updateCartCount();renderCart();
